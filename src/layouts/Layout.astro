---
interface Props {
  title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="Fluidity Marine — Active Boundary Layer Thrust Vectoring. The Operating System of the Water." />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&display=swap" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;500;600;700&display=swap" rel="stylesheet" />
    <link href="/css/electric-design.css" rel="stylesheet" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
  </head>
  <body>
    <nav>
      <div class="nav-content">
        <a href="/" class="logo-link">FLUIDITY<span class="logo-accent">MARINE</span></a>
        <div class="links">
          <a href="/technology">Technology</a>
          <a href="/infographics">Infographics</a>
          <a href="/about">About</a>
          <a href="/investors">Investors</a>
          <a href="/press">Press</a>
          <a href="/contact">Contact</a>
          <a href="https://wefunder.com/fluidity" class="nav-button">Invest Now</a>
        </div>
      </div>
    </nav>
    <slot />
    <footer>
      <div class="footer-content">
        <div class="footer-brand">
          <strong class="footer-logo">FLUIDITY MARINE</strong>
          <p>A division of InnovativeDynamix</p>
        </div>
        <div class="footer-links">
          <a href="/technology">Technology</a>
          <a href="/infographics">Infographics</a>
          <a href="/about">About</a>
          <a href="/investors">Investors</a>
          <a href="/press">Press</a>
          <a href="/contact">Contact</a>
        </div>
        <div class="footer-social">
          <span>&copy; 2026 InnovativeDynamix. All rights reserved.</span>
        </div>
      </div>
    </footer>
  </body>
</html>

<style is:global>
  :root {
    --cyan: #00F0FF;
    --cyan-dim: #0099A8;
    --cyan-glow: rgba(0, 240, 255, 0.15);
    --amber: #FFB800;
    --amber-hover: #FFD040;
    --accent: #00F0FF;
    --bg-deep: #0A0A0F;
    --surface: #1A1A2E;
    --surface-card: #13131C;
    --bg-elevated: #1C1C2A;
    --bg: #0A0A0F;
    --bg-card: rgba(19, 19, 28, 0.6);
    --bg-glass: rgba(10, 10, 15, 0.85);
    --text: #E2E8F0;
    --text-muted: #8899B4;
    --text-secondary: #A0A0B5;
    --border: rgba(0, 240, 255, 0.1);
    --border-light: rgba(255, 255, 255, 0.06);
    --border-cyan: rgba(0, 240, 255, 0.15);
    --gradient-ocean: linear-gradient(135deg, #00F0FF 0%, #00CCE0 100%);
    --gradient-electric: linear-gradient(to right, #00F0FF, #00CCE0);
    --shadow-ocean: rgba(0, 240, 255, 0.2);
    --radius-sm: 8px;
    --radius-md: 12px;
    --radius-lg: 20px;
    --radius-xl: 24px;
    --radius-2xl: 32px;
    --font-display: 'Space Grotesk', 'Inter', sans-serif;
    --font-body: 'Inter', system-ui, -apple-system, sans-serif;
    --glow-cyan-sm: 0 0 6px rgba(0, 240, 255, 0.15);
    --glow-cyan-md: 0 0 12px rgba(0, 240, 255, 0.2);
    --glow-cyan-lg: 0 0 24px rgba(240, 255, 0.3);
    --glow-amber-sm: 0 0 6px rgba(255, 184, 0, 0.2);
    --glow-amber-md: 0 0 12px rgba(255, 184, 0, 0.3);
  }

  * { box-sizing: border-box; }

  html {
    font-family: var(--font-body);
    background: var(--bg-deep);
    color: var(--text);
    scroll-behavior: smooth;
  }

  body {
    margin: 0;
    min-height: 100vh;
  }

  ::selection {
    background: rgba(0, 240, 255, 0.3);
    color: #fff;
  }

  /* ─── Navigation ─── */
  nav {
    border-bottom: 1px solid var(--border-cyan);
    padding: 1rem 0;
    position: sticky;
    top: 0;
    background: linear-gradient(180deg, rgba(10, 10, 15, 0.95) 0%, rgba(7, 14, 26, 0.9) 100%);
    backdrop-filter: blur(16px);
    -webkit-backdrop-filter: blur(16px);
    z-index: 100;
  }

  .nav-content {
    max-width: 1100px;
    margin: 0 auto;
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0 2rem;
  }

  .logo-link {
    font-weight: 800;
    font-size: 1.35rem;
    text-decoration: none;
    color: #fff;
    letter-spacing: 0.08em;
    font-family: var(--font-display);
  }

  .logo-accent {
    color: var(--amber);
    margin-left: 2px;
  }

  .links { display: flex; gap: 2rem; align-items: center; }

  .links a {
    text-decoration: none;
    color: var(--text-muted);
    font-weight: 500;
    font-size: 0.9rem;
    transition: all 0.2s ease;
    position: relative;
  }

  .links a:hover { color: var(--cyan); text-shadow: 0 0 12px rgba(0, 240, 255, 0.35); }

  .links a::after {
    content: '';
    position: absolute;
    bottom: -4px;
    left: 0;
    width: 0;
    height: 2px;
    background: var(--gradient-ocean);
    transition: width 0.2s ease;
  }

  .links a:hover::after { width: 100%; }

  .nav-button {
    background: var(--gradient-ocean) !important;
    color: #050a0f !important;
    padding: 0.6rem 1.3rem;
    border-radius: var(--radius-sm);
    font-weight: 700 !important;
    box-shadow: var(--glow-cyan-sm);
    transition: all 0.2s !important;
  }

  .nav-button:hover {
    transform: translateY(-1px);
    box-shadow: var(--glow-cyan-md) !important;
  }

  .nav-button::after { display: none !important; }

  /* ─── Footer ─── */
  footer {
    border-top: 2px solid var(--border-cyan);
    padding: 3rem 2rem;
    background: linear-gradient(180deg, transparent 0%, rgba(10, 10, 15, 0.6) 100%);
  }

  .footer-content {
    max-width: 1100px;
    margin: 0 auto;
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
    gap: 2rem;
  }

  .footer-logo {
    color: var(--amber);
    font-size: 0.95rem;
    letter-spacing: 0.06em;
  }

  .footer-brand p {
    color: var(--text-muted);
    font-size: 0.8rem;
    margin: 0.3rem 0 0;
  }

  .footer-links {
    display: flex;
    gap: 1.5rem;
  }

  .footer-links a {
    color: var(--text-muted);
    text-decoration: none;
    font-size: 0.85rem;
    transition: all 0.2s ease;
  }

  .footer-links a:hover { color: var(--cyan); text-shadow: 0 0 8px rgba(0, 240, 255, 0.3); }

  .footer-social span {
    color: var(--text-muted);
    font-size: 0.8rem;
  }

  /* ─── Utility ─── */
  .electric-text {
    background: var(--gradient-electric);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .gold-text {
    background: linear-gradient(135deg, var(--amber) 0%, var(--amber-hover) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .ocean-text {
    color: var(--cyan);
  }

  .page {
    max-width: 1100px;
    margin: 0 auto;
    padding: 6rem 2rem;
  }

  .page-title {
    font-size: clamp(2.5rem, 6vw, 4.5rem);
    font-weight: 900;
    line-height: 1.05;
    margin: 0 0 1.5rem;
    color: #fff;
    letter-spacing: -0.03em;
    font-family: var(--font-display);
  }

  .page-subtitle {
    font-size: 1.2rem;
    color: var(--text-muted);
    margin: 0 0 4rem;
    max-width: 650px;
    line-height: 1.7;
  }

  .card {
    background: var(--bg-card);
    border: 1px solid var(--border);
    padding: 2.5rem;
    border-radius: var(--radius-xl);
    transition: all 0.3s ease;
  }

  .card:hover {
    border-color: rgba(0, 240, 255, 0.2);
    background: rgba(255, 255, 255, 0.05);
    transform: translateY(-2px);
  }

  .card h2, .card h3 {
    font-size: 1.4rem;
    color: #fff;
    margin: 0 0 1rem;
  }

  .card p {
    color: var(--text-muted);
    line-height: 1.7;
    margin: 0;
  }

  .card-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
    gap: 1.5rem;
    margin-bottom: 5rem;
  }

  .button {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.9rem 2rem;
    border-radius: var(--radius-md);
    font-weight: 700;
    text-decoration: none;
    transition: all 0.25s ease;
    cursor: pointer;
    border: none;
    font-size: 0.95rem;
  }

  .button-primary {
    background: var(--gradient-ocean);
    color: #050a0f;
    box-shadow: 0 8px 25px var(--shadow-ocean);
  }

  .button-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 12px 35px rgba(0, 240, 255, 0.35);
  }

  .button-secondary {
    background: rgba(255, 255, 255, 0.05);
    color: #fff;
    border: 1px solid var(--border);
  }

  .button-secondary:hover {
    background: rgba(255, 255, 255, 0.1);
    transform: translateY(-2px);
  }

  .section-title {
    font-size: clamp(1.8rem, 4vw, 2.5rem);
    font-weight: 800;
    margin: 0 0 1rem;
    color: #fff;
    font-family: var(--font-display);
  }

  .section-subtitle {
    color: var(--text-muted);
    font-size: 1.05rem;
    margin-bottom: 3rem;
    max-width: 600px;
    line-height: 1.7;
  }

  .divider {
    border: none;
    height: 1px;
    background: var(--border-light);
    margin: 5rem 0;
  }

  .glass-box {
    background: var(--bg-glass);
    backdrop-filter: blur(16px);
    -webkit-backdrop-filter: blur(16px);
    border: 1px solid var(--border);
    padding: 2.5rem;
    border-radius: var(--radius-xl);
  }

  /* ─── Animations ─── */
  @keyframes fadeInUp {
    from { opacity: 0; transform: translateY(30px); }
    to { opacity: 1; transform: translateY(0); }
  }

  @keyframes fadeIn {
    from { opacity: 0; }
    to { opacity: 1; }
  }

  @keyframes pulse-glow {
    0%, 100% { box-shadow: 0 0 0 0 rgba(0, 240, 255, 0.6); }
    50% { box-shadow: 0 0 0 15px rgba(0, 240, 255, 0); }
  }

  .animate-in {
    animation: fadeInUp 0.6s ease forwards;
    opacity: 0;
  }

  .delay-1 { animation-delay: 0.1s; }
  .delay-2 { animation-delay: 0.2s; }
  .delay-3 { animation-delay: 0.3s; }
  .delay-4 { animation-delay: 0.4s; }
  .delay-5 { animation-delay: 0.5s; }

  /* ─── Responsive ─── */
  @media (max-width: 768px) {
    .links a:not(.nav-button) { display: none; }
    .card-grid { grid-template-columns: 1fr; }
    .page { padding: 4rem 1.5rem; }
    .footer-content { flex-direction: column; text-align: center; }
  }
</style>
